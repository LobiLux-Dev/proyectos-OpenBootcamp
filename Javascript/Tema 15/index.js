let name = "Luis"
let lastName = "Sanchez"
let object = {
    name,
    lastName
}

// sessionStorage.setItem("object", JSON.stringify(object))
// localStorage.setItem("object", JSON.stringify(object))
// document.cookie = "object=" + JSON.stringify(object) + "; expires=" + new Date(Date.now() + (2 * 60 * 1000)).toUTCString()